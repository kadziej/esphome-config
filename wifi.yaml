---
wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  manual_ip:
    static_ip: "192.168.10.${devicenumber}"
    gateway: 192.168.10.1
    subnet: 255.255.255.0
  fast_connect: False
  reboot_timeout: 15min
  #power_save_mode: NONE
  use_address: "192.168.10.${devicenumber}"
